# Project Customizations

## Build and Commit Process
When a change is ready to commit, you must ALWAYS run a build (e.g. `npm run build`) before committing, so that the rebuilt files in the `dist` folder are included in the same commit.
