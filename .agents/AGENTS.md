# Project Customizations

## Build and Commit Process
When a change is ready, you must ALWAYS run a build (e.g. `npm run build`) so that the rebuilt files in the `dist` folder are updated. Do NOT commit the changes yourself; let the human user handle the git commit.
